server:
  port: 8080

spring:
  application:
    name: lms-backend
  
  datasource:
    url: jdbc:postgresql://localhost:5432/lms_db
    username: postgres
    password: Skerdi199
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

# JWT Configuration
jwt:
#  secret: lms-secret-key-for-jwt-token-generation-must-be-at-least-256-bits-long
  secret: dGhpcy1pcy1hLXZlcnktc2VjdXJlLWxtcy1qd3Qtc2VjcmV0LWtleS0yNTY=
  expiration: 86400000

# Stripe Configuration
stripe:
  api-key: sk_test_your_stripe_secret_key
  webhook-secret: whsec_your_webhook_secret

# OpenAI Configuration
openai:
  api-key: sk-your-openai-api-key

# File Upload Configuration
file:
  upload-dir: ./uploads

# CORS Configuration
cors:
  allowed-origins: http://localhost:4200

logging:
  level:
    com.lms: DEBUG
    org.springframework.security: DEBUG